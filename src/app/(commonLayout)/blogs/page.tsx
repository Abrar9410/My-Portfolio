import { Metadata } from "next";


export const metadata: Metadata = {
    title: "",
    description: ""
};


const BlogsPage = () => {
    return (
        <div>
            BLOGS
        </div>
    );
};

export default BlogsPage;