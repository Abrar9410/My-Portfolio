import { Metadata } from "next";


export const metadata: Metadata = {
    title: "Profile",
    description: ""
};


const ProfilePage = () => {
    return (
        <div>
            PROFILE
        </div>
    );
};

export default ProfilePage;